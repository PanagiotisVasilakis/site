╔══════════════════════════════════════════════════════════════════╗
║                    🎯 QUICK SETUP - RUN THIS                     ║
╚══════════════════════════════════════════════════════════════════╝

cd /home/pvs/site
./scripts/install-postgres-and-setup.sh

Then restart terminal or run: source ~/.bashrc

That's it! The script will:
✓ Install PostgreSQL
✓ Create test database
✓ Run migrations
✓ Set up environment variables

═══════════════════════════════════════════════════════════════════

YOUR COMMAND WILL THEN WORK:

NODE_ENV=test npx tsx -e "import { randomUUID } from 'crypto'; \
import { prisma } from './src/lib/prisma'; \
prisma.user.create({ data: { id: randomUUID(), phoneE164: '+123', \
countryOrigin: 'GR', email: null, passwordHash: null }}).then(u => { \
console.log('User:', u); return prisma.\$disconnect(); });"

═══════════════════════════════════════════════════════════════════

WHAT WAS FIXED:
❌ Before: id: 'test' (invalid UUID) + pg-mem incompatibility
✅ After:  id: randomUUID() (valid) + real PostgreSQL

═══════════════════════════════════════════════════════════════════

MORE INFO:
- docs/NEXT_STEPS.md          - This guide expanded
- docs/INSTALL_POSTGRESQL.md  - Manual installation steps
- docs/TEST_DATABASE_SETUP.md - Complete reference

═══════════════════════════════════════════════════════════════════
